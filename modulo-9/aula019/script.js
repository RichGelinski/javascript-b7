let somar = (a,b) => a + b //maneira compacta para declarar arrow function
console.log(somar(4,6))

let nomeCompleto = (n) => { //maneira completa para declarar arrow function
    return n.length
}
console.log(nomeCompleto('Felipe'))